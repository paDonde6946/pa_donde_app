import 'package:flutter/material.dart';

class AccesoGPSPag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AccesoGpsPage'),
      ),
    );
  }
}
